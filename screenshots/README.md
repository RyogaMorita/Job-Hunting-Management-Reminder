# App Store 用スクリーンショット

ここに元画像を置き、以下を実行すると `out/` に規格どおりのものが出ます。

```
python3 tools/fit_screenshots.py screenshots
```

- 出力は 1290x2796（6.9インチ枠）・透過なし
- 比率が違うものは切らずに余白を足す（上部のコピーが欠けないようにするため）
- ファイル名の順に 01_, 02_ … と番号が付く。App Store Connect での並び順に合わせて名前を付けること
